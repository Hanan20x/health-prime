name: keep-alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend to prevent Render free-tier spin-down
        run: curl -sf --max-time 60 https://health-prime-api.onrender.com/ || true
