services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: healthprime
    ports:
      - "5432:5432"
    volumes:
      - healthprime_pg:/var/lib/postgresql/data

volumes:
  healthprime_pg:
